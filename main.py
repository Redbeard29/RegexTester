#Goal is to write a program that accepts a regex pattern and a corresponding string that represents a barcode, and to determine
#whether or not the barcode matches the regex. If it does not match, the program will return a list of the characters that 
#failed the pattern.